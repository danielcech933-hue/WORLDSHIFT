# ROBLOX FORGE Desktop

The desktop shell is the operator workspace for Forge. It controls the local development stack and exposes the AI Team, diagnostics and Studio state through the Forge bridge.

Keep process execution inside the trusted desktop main process; renderer code only uses the preload API.
