# AI Team workspace

The desktop AI workspace is designed around supervised roles: Architect, Coder, Reviewer, Researcher and Tester. The Orchestrator selects an installed agent for each role and executes one-shot tasks against the selected project.

The UI must surface the current project, Studio state, diagnostics, task prompt, role, approval state and result without hiding failures.
